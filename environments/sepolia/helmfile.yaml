helmDefaults:
  createNamespace: true

releases:
  - name: blobscan
    namespace: sepolia
    chart: blobscan/blobscan
    version: 0.10.0
    values:
      - values.yaml
    secrets:
      - secrets.yaml
