helmDefaults:
  createNamespace: true

releases:
  - name: blobscan
    namespace: sepolia
    chart: blobscan/blobscan
    version: 0.9.21
    values:
      - values.yaml
    secrets:
      - secrets.yaml
