helmDefaults:
  createNamespace: true

releases:
  - name: blobscan
    namespace: sepolia
    chart: blobscan/blobscan
    version: 0.11.4
    values:
      - values.yaml
    secrets:
      - secrets.yaml

  - name: ipfs-gateway
    namespace: sepolia
    chart: ../../kubernetes/helm/ipfs-gateway
    values:
      - ingress:
          host: sepolia.blobscan.com
          tlsSecretName: sepolia-blobscan-tls
