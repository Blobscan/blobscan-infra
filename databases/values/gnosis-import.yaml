mode: recovery

recovery:
  method: import

  import:
    type: microservice
    databases:
      - blobscan_gnosis_prod
    source:
      database: blobscan_gnosis_prod
      host: blobscan-gnosis-prod-do-user-2001904-0.c.db.ondigitalocean.com
      port: 25060
      # verify-ca and verify-full require ssl certs
      sslMode: require
      username: "doadmin"
      passwordSecret:
        name: do-migration-db
        key: password
