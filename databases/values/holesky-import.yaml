mode: recovery

recovery:
  method: import

  import:
    type: microservice
    databases:
      - blobscan_holesky_prod
    source:
      database: blobscan_holesky_prod
      host: blobscan-holesky-prod-do-user-2001904-0.c.db.ondigitalocean.com
      port: 25060
      # verify-ca and verify-full require ssl certs
      sslMode: require
      username: "doadmin"
      passwordSecret:
        name: do-migration-db
        key: password
